import 'package:flutter/material.dart';

class KkomulPage extends StatelessWidget {
  const KkomulPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('꼬물이 페이지'),
    );
  }
}
