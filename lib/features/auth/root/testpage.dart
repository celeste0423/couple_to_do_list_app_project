import 'package:flutter/cupertino.dart';

class testpage extends StatelessWidget {
  final userdata;
  const testpage(this.userdata, {super.key});

  @override
  Widget build(BuildContext context) {
    return Text('testpage');
  }
}
